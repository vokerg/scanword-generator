name: UI table contract

on:
  pull_request:
    branches:
      - main
    paths:
      - "ui.js"
      - "tools/ui-table-accessibility-test-v1.cjs"
      - ".github/workflows/ui-table-contract.yml"
  push:
    branches:
      - main
    paths:
      - "ui.js"
      - "tools/ui-table-accessibility-test-v1.cjs"
      - ".github/workflows/ui-table-contract.yml"
  workflow_dispatch:

concurrency:
  group: ui-table-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  contract:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check out exact candidate head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Validate UI table contract syntax
        run: |
          node --check ui.js
          node --check tools/ui-table-accessibility-test-v1.cjs

      - name: Verify UI table contract
        run: node tools/ui-table-accessibility-test-v1.cjs
