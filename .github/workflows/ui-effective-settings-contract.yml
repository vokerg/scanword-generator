name: UI effective settings contract

on:
  pull_request:
    branches:
      - main
    paths:
      - "index.html"
      - "ui.js"
      - "tools/ui-effective-settings-test-v1.cjs"
      - ".github/workflows/ui-effective-settings-contract.yml"
  push:
    branches:
      - main
    paths:
      - "index.html"
      - "ui.js"
      - "tools/ui-effective-settings-test-v1.cjs"
      - ".github/workflows/ui-effective-settings-contract.yml"
  workflow_dispatch:

concurrency:
  group: ui-effective-settings-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  contract:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check out exact candidate head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Validate settings contract syntax
        run: |
          node --check ui.js
          node --check tools/ui-effective-settings-test-v1.cjs

      - name: Verify effective generation settings
        run: node tools/ui-effective-settings-test-v1.cjs
