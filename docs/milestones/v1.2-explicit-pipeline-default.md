# Milestone 1.2 — Explicit production pipeline

Date: 2026-07-23

Status: accepted draft-project baseline after Phase 9 merge.

## Decision

Promote the directly ordered explicit pipeline to the canonical browser and Node production path while retaining the former cumulative wrapper chain as an explicit rollback source.

This milestone changes orchestration ownership, not selected-grid output.

## Canonical defaults

```text
SCANWORD_EXPLICIT_PIPELINE=on
SCANWORD_PIPELINE_STAGE_RUNTIME=explicit
SCANWORD_WRAPPER_INSTALLATION_LOCK=explicit-pipeline-v1
SCANWORD_FULL_CORPUS_RETRIEVAL=off
SCANWORD_CLUE_FEASIBILITY=off
SCANWORD_PARTIAL_SEARCH=off
```

Rollback:

```text
SCANWORD_EXPLICIT_PIPELINE=off
```

## Ownership

```text
active generateBest owner: construction-pipeline-v1
execution owner:            direct-production-stage-runtime-v2
rollback owner:             legacy-wrapper-chain
```

The directly ordered production source executes the accepted vocabulary portfolio, clue-repair, victim-repair, baseline-guard and editorial-repair stages without allowing later modules to replace the global production entry point.

## Promotion evidence

Exact explicit-versus-rollback output parity:

| seed set | passed | failures | runtime ratio |
| --- | ---: | ---: | ---: |
| development-20 | 20/20 | 0 | 0.9692 |
| promotion-50 | 50/50 | 0 | 0.9850 |
| stability-100 | 100/100 | 0 | 0.9915 |

Every pair had identical grid, placed-answer, clue and geometry digests. Every result was valid, connected and exact-clue-only. The aggregate runtime gate was 1.10.

Evidence head and durable ref:

```text
f77be9fed8223925819830fad6956f1018717bbb
refs/heads/research/archive-phase-9-explicit-default-evidence-2026-07-23
```

Workflow and artifacts:

```text
run: 30021580067
development: 8569741999
promotion:   8570088676
stability:   8570371023
```

The complete ledger is `research/explicit-default/README.md`.

## Preserved baseline

The corpus and selected outputs remain the locked vocabulary-first v8 baseline:

- 40,966 attributed entries;
- deterministic 2,500/3,500 active-set portfolio;
- complete clue allocation and repair;
- same-geometry editorial cleanup;
- panel-first complete candidate comparison;
- unchanged complete structural validator.

Phase 9 makes no zero-panel or publication-quality clue claim.

## Accepted debt

- Average residual panels remain near five on locked sets.
- Bounded beam search remains opt-in because its accepted density gain is too expensive for the default.
- Full-corpus pattern fallback and clue-feasibility estimation remain research features.
- The complete-pipeline Pareto frontier from the original roadmap remains deferred.
- Historical repair internals are directly ordered but not all are pure CandidateState implementations.
- Selected-grid clue prose is truthful but not publication edited.

## Next investigation

Return to the deferred complete-pipeline frontier using the new explicit ownership boundary. Compare bounded finalists only after exact clue allocation, repair, editorial cleanup and complete validation. Preserve the exact production baseline as a candidate and rollback.
